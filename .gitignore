# Dependencies
node_modules/
.pnp/
.pnp.js

# Build
dist/
build/
.next/
out/

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Backend
backend/node_modules/
backend/dist/

# Mavis runtime state (per-session planning / scratchpads)
.mavis/

# Stray logo upload path (artifacts accidentally created during testing)
backend/frontend/

# Test PDFs generated during invoice layout debugging.
# These are created by `node -e "...generateInvoicePDF(...)"` calls
# and live at the repo root. They're not part of the app — the
# real PDF goes through the HTTP API. Keep them out of git so
# `git status` stays clean during PDF iteration.
test_invoice_*.pdf
test_zugferd_*.pdf
